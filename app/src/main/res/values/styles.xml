<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowShowWallpaper">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!-- Splash Screen Theme -->
    <style name="SplashTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@drawable/harmonia_icon</item>
        <item name="postSplashScreenTheme">@style/AppTheme</item>
        <item name="windowSplashScreenBackground">@color/white</item>
    </style>

    <!-- Lock Activity Theme -->
    <style name="LockActivityTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowShowWallpaper">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:navigationBarColor">@color/lock_activity_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!-- Settings Activity Theme -->
    <style name="SettingsActivityTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowShowWallpaper">false</item>
        <item name="android:windowBackground">@color/settings_activity_background</item>
        <item name="android:navigationBarColor">@color/settings_activity_background</item>
        <item name="android:statusBarColor">@color/settings_activity_background</item>
    </style>
</resources>